# API Configuration
# Base URL for backend API endpoints (required)
# Version: v1.0.0
VITE_API_BASE_URL=http://localhost:8000/api/v1

# API request timeout in milliseconds (optional)
# Default: 30000ms (30 seconds)
# Range: 1000-60000ms
VITE_API_TIMEOUT=30000

# Blitzy Enterprise Authentication
# Client ID for Blitzy platform (required)
# Security: Critical - Rotate every 90 days
# Format: UUID v4
VITE_BLITZY_CLIENT_ID=00000000-0000-0000-0000-000000000000

# Enterprise SSO Configuration
# SSO Provider for Blitzy Enterprise (required)
# Must match approved provider list
VITE_BLITZY_SSO_PROVIDER=blitzy-enterprise

# SAML 2.0 Endpoint (required)
# Must use HTTPS with valid SSL certificate
VITE_BLITZY_SAML_ENDPOINT=https://sso.blitzy-enterprise.com/saml/v2

# Session Management
# Session timeout in seconds (optional)
# Default: 1800s (30 minutes)
# Range: 300-3600s
VITE_AUTH_SESSION_TIMEOUT=1800

# Token refresh interval in seconds (optional)
# Default: 240s (4 minutes)
# Must be less than session timeout
VITE_AUTH_TOKEN_REFRESH=240

# UI Configuration
# Default theme for Blitzy Page Builder (optional)
# Options: 'light' or 'dark'
VITE_BLITZY_THEME=light

# Security Features
# Multi-factor authentication (optional)
# Required to be 'true' in production
VITE_ENABLE_MFA=true

# Environment Type
# Options: 'development', 'production', 'test'
NODE_ENV=development